PORT=5000

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mariadb_password
DB_NAME=shop_pulse

JWT_SECRET=replace_this_with_a_long_random_secret_string

FRONTEND_URL=http://localhost:3000

# Optional — for real password-reset emails. Leave blank and the
# forgot-password endpoint falls back to returning the reset link
# directly in the response (dev-only, not for production).
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=